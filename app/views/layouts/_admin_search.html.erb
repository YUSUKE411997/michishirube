<div class="search-form">
  <%= form_with url: admin_search_path, method: :get, local: true do |f| %>
    <%= f.search_field :word, placeholder: "ユーザーまたは投稿" %>
    <%= f.select :range, options_for_select([['ユーザー'],['投稿']])%>
    <%= f.submit '検索' %>
  <% end %>
</div>