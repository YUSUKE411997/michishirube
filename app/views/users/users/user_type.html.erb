<div>
  <div class="container">
    <%= render 'profile', user: @user %>
    <h3><%= @type_name %></h3>
    <% if @posts.blank? %>
      <p>投稿はありません</p>
    <% else %>
      <%= render 'users/posts/index', posts: @posts %>
    <% end %>
  </div>
</div>
