<thead>
  <tr>
    <th></th>
    <th>投稿種類</th>
    <th>投稿タイトル</th>
    <th>投稿内容</th>
    <th>コメント数</th>
    <th>いいね数</th>
    <th>投稿時間</th>
  </tr>
</thead>
<div class="jscroll">
  <tbody>
    <% posts.each do |post| %>
      <tr>
        <td><%= link_to user_path(post.user) do %>
          <%= attachment_image_tag post.user, :profile_image, :fill, 50, 50, fallback: "profile/no_image.jpg",  size: "50x50" %><br>
          <%= post.user.name.truncate(10) %>
          <% end %>
        </td>
        <td><%= link_to post.type, types_path(type: post.type) %></td>
        <td><%= link_to post.title.truncate(12), post_path(post.id) %></td>
        <td><%= post.body.truncate(25) %></td>
        <td><%= post.comments.size %>件</td>
        <td>💖<%= post.likes.size %></td>
        <td><%= post_created(post) %></td>
      </tr>
    <% end %>
    <div>
      <%= link_to_next_page posts, '次のページ', class: "next"%>
    </div>
  </tbody>
</div>
